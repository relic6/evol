# 《EVOL 设计哲学与原则》

> PRINCIPLES.md · 这是 EVOL 日常开发决策的「判断基准」。
>
> 当 VISION 告诉我们"**要去哪里**"，PRINCIPLES 告诉我们"**在每一个分岔路口怎么选**"。
> 当你纠结一个功能要不要加、一个 API 应该怎么设计、一个折中应该往哪边偏——回到这里查一遍。

---

## 第一性原理：成长必须可信赖

EVOL 服务的是「软件的成长」，但成长本身是一种**受控的不确定性**。
这意味着 EVOL 必须比普通框架**更克制、更透明、更可逆**。

- 比普通框架更**克制**——因为我们处理的是会自己变化的系统
- 比普通框架更**透明**——因为成长必须能被人看见
- 比普通框架更**可逆**——因为没有回滚的成长就是漂移

下面所有原则，都是从这一句话演化出来的不同侧面。

---

## 原则一：克制优于强大（Restraint over Richness）

**正面**：EVOL 提供「最小够用」的能力。每一个核心能力都要被反复打磨，宁可少做一个功能，不要多塞一个抽象。新功能必须**证明自己值得加入**，而不是被默认接受。

**反例**：不要为了"功能丰富"去堆 Reflector 的多种反思策略、Memory 的多种存储后端、Advisor 的多种触发器。**任何能用一种方式解决的问题，不应该提供两种**。

**为何重要**：成长性本身就是一个野心极大的命题——能力越多、组合越复杂，框架越容易失控。克制是 EVOL 抗熵增的核心防线，也是它能不变成另一个 LangChain 的关键。

---

## 原则二：可读优于黑盒（Readable over Opaque）

**正面**：所有「进化产物」——画像、规则、洞察、记忆、提示词模板——都必须是**人类可读、可编辑、可手工修改**的（YAML/JSON 明文，纯文本 prompt）。打开 `.evol/` 目录的任何一个文件，开发者都能看懂、能改对、能 diff。

**反例**：不要把用户偏好藏进向量库的 384 维浮点中，让人无法理解、无法纠正。不要把反思结论藏进 LLM 的隐藏权重里，让人无法回溯、无法解释。

**为何重要**：可读性是可信赖的前提。一个开发者无法"看见"自己产品在学什么，就**无法对它的行为负责**。可读性也是冷启动、调试、迁移、审计、跨团队协作的共同基础。

---

## 原则三：可关闭优于全自动（Off-able over Always-on）

**正面**：任何成长行为都必须能**一键关闭**。开发者要能在运行时把 EVOL 冻结成"只服务任务、不再积累 / 不再反思 / 不再启发"的状态。一个 flag 即可。

**反例**：不要把成长能力做成"无法绕过"的强依赖。不要让接入 EVOL 的产品在 EVOL 出问题时也跟着停摆。不要让"反思"成为产品功能链路上的关键路径节点。

**为何重要**：**带不带成长性，是产品的运行时配置，不是架构选择。** 这是企业级框架的基本要求，也是 EVOL 区别于"研究项目"的关键证明。

---

## 原则四：版本化优于实时性（Versioning over Immediacy）

**正面**：所有进化产物（提示词、规则、画像、记忆）都必须**版本化**，必须可以回滚到任何历史状态。每一次"沉淀"都要带上**时间、来源、置信度、可追溯的证据链**。

**反例**：不要为了响应快而直接覆盖记忆。不要为了"自动学习"省略版本管理。不要让"昨天还好好的产品，今天突然变得很怪"成为可能。

**为何重要**：进化是「可控的不确定性」——而控制的核心手段就是**版本与回滚**。这是 Harness 思维在成长层的延伸：执行有 retry 与 rollback，成长也必须有。

---

## 原则五：适应性优于类人（Adaptive over Anthropomorphic）

**正面**：EVOL 的根基是「适应性系统的通用学习闭环」（生物进化、科学方法、组织学习、机器学习的共同结构），**不是**「模仿人脑」。我们借鉴人类认知中的精华机制（叙事记忆、类比迁移、双系统响应），但不承诺"像人"。

**反例**：不要在文档与 API 命名中使用"它在思考""它有意识""它能感受"这样的拟人化措辞。不要为了追求"像人"去引入未被工程化的模糊概念（直觉、潜意识、情绪）。

**为何重要**：以"人类思维"为蓝本，等于把地基建在**还在争论中的认知科学**之上。把 EVOL 定位为"为软件赋予适应性"而非"造类人 AI"，能避开几十年来 SOAR、ACT-R、OpenCog 反复栽进去的同一个坑。

---

## 原则六：本地优先优于云依赖（Local-first over Cloud-bound）

**正面**：EVOL = 一个本地库 + 一份接入规范 + 一个本地存储约定（`.evol/` 目录）。所有成长资产默认**存在产品本地，由产品自己持有**——可 git 管理、可审计、可迁移、可加密。

**反例**：不要把"必须连服务端"做成默认依赖。不要把用户的成长资产沉淀到外部 SaaS。不要让"接入 EVOL"等于"让出数据主权"。

**为何重要**：企业用户友好、个人开发者友好、隐私友好。本地优先**并不排斥云**——但云必须是"可选增强"，而不是"必选基座"。

---

## 原则七：窄而深优于宽而浅（Narrow-deep over Wide-shallow）

**正面**：MVP 阶段只做**两层成长**：知识进化（必做） + 启发反哺（差异化关键）。把这两层做到工程级稳定后，再向策略进化、能力进化、元认知层等更深维度扩展。

**反例**：不要在 v0.1 就承诺"全维度成长"。不要为了路演 demo 好看，把四个层次都做一遍但每一层都只做了 30%。不要把"框架能做什么"和"框架已经能做好什么"混为一谈。

**为何重要**：所有成功的框架（Harness、React、Spring）都是从**一个极小的核心做扎实**开始的。野心可以大，落地必须窄。先做"窄而深"，再做"宽而浅"——顺序一旦颠倒，框架就死了。

---

## 原则八：启发用户作为一等公民（Inspire as First-class Citizen）

**正面**：EVOL 把「反向启发使用者」作为框架的**核心能力之一**，与「服务任务」同等重要。Inspiration Layer 不是某个产品的附加 feature，而是 EVOL 工程上的一等公民——有专属的 API、专属的触发机制、专属的输出契约。

**反例**：不要让"启发"沦为某个 demo 里的彩蛋功能。不要让 Advisor 只优化任务输出而忽略对用户的洞见输出。不要把"大师感"做成 prompt 工程的副产品。

**为何重要**：这是 EVOL 区别于**所有现有 Agent 框架**（Reflexion、LangGraph、AutoGen、DSPy/GEPA）的核心差异化。如果失去这一点，EVOL 就只是又一个"自我改进 Agent"——而不是我们承诺过的「成长基础设施」。

---

## 原则九：价值锚点不可变（Anchored Values, Evolving Strategies）

**正面**：开发者通过 `evol.config.yaml` 显式声明产品的「价值锚点」——不可演化的核心原则。EVOL 在所有反思、沉淀、启发中将其作为**不可逾越的硬约束**。可以演化的，只是策略与表达；不可以演化的，是底线与初心。

**反例**：不要让 LLM 在反思时"漂移"出与产品价值观冲突的结论。不要让"自我进化"成为"自我背离"的伪装。不要让 Memory 的某次错误更新覆盖掉框架级的安全约束。

**为何重要**：这是把 Constitutional AI 的思路**工程化下放到产品层**。没有锚点的进化，是一种自由落体——我们要的是螺旋上升，不是无方向的随机游走。

---

## 原则十：接入成本即设计标尺（Integration Cost as the Ruler）

**正面**：开发者接入 EVOL 的工作量必须控制在**半天以内**——一份 config、三个埋点（任务开始 / 任务结束 / 用户反馈）、一处提示词增强。任何让接入变难的设计，都被视为对原则的破坏，需要重新设计。

**反例**：不要要求开发者继承基类、改造架构、迁移存储。不要让"成为成长型产品"听起来像"重写一遍代码"。不要把繁琐的"配置 50 个字段"伪装成"灵活性"。

**为何重要**：框架的成败不在能力多寡，而在"开发者多快能用上、多容易留下"。**接入成本是 EVOL 的真正护城河**——也是它区别于研究项目的硬指标。一份接入示例如果写得别扭，那不是文档不好，是设计错了。

---

## 元原则：当原则之间冲突时

设计原则会冲突。
"可读优于黑盒"会和"性能极致"冲突；
"克制优于强大"会和"用户的需求清单"冲突；
"启发用户"会和"不要打扰"冲突。

冲突是设计的常态。处理冲突的方法是：

**第一步**：先回到 VISION。哪个选择更接近「软件成长 + 启发用户」这个北极星？
**第二步**：再看「成长可信赖」三原则——可读、可关闭、可回滚。选项是否同时满足？
**第三步**：最后看「接入成本」。这个改动会不会让开发者的接入工作变难？

如果一个设计**同时违反多条原则**，它几乎一定是错的，无论它看起来多么诱人。

---

## 一份"反原则清单"：EVOL 不允许的设计倾向

把上面所有原则反过来表述，就是 EVOL 永远不会接受的设计倾向：

- 「我们再加一个抽象层会更优雅」 —— 违反**克制**
- 「这个用 embedding 存就好，不用让用户看得懂」 —— 违反**可读**
- 「成长应该是默认开启的，不能让用户关」 —— 违反**可关闭**
- 「这次更新就直接覆盖吧，没必要保留历史」 —— 违反**版本化**
- 「我们让它表现得更像人一点」 —— 违反**适应性**
- 「先做 SaaS，再考虑本地版」 —— 违反**本地优先**
- 「v1 就把四层成长全做了」 —— 违反**窄而深**
- 「启发功能放到下一版再做吧」 —— 违反**启发一等公民**
- 「让 LLM 自己判断要不要遵守这个约束」 —— 违反**价值锚点**
- 「再多让用户配置几个字段就行了」 —— 违反**接入成本**

每当出现以上语句，请自动触发警觉。

---

## 写在最后

这份文档的价值不在它「很完整」，而在它**足够锐利**——
锐利到能在你犹豫的时刻，提醒你应该选哪条路。

PRINCIPLES 不是一次写完的。在 EVOL 的演化过程中，每一次重要的设计决策、每一次踩坑后的反思，都可能沉淀为新的原则。这份文档会持续生长。

但有一点不会变：

> **原则的目的是约束自己，不是约束他人。**

我们写下这些原则，不是为了挑剔接入 EVOL 的产品，
而是为了在每一次自己面对诱惑时，能多看一眼这份"宪法"。

> Harness 让事情**做对**。
> EVOL 让事情**做得越来越好**。
> PRINCIPLES 让我们**始终知道，"越来越好"指的是哪个方向**。

---

## 附：版本与迭代

- **v0.1（本版）** —— 起步阶段原则定稿，10 条核心原则
- 每新增一条原则，需附一段简短的"催生案例"（什么样的踩坑让我们决定加上它）
- 删除或修改原则需要严格的理由：每一次修订都是 EVOL 设计哲学的一次位移
